<div class="container"><% @restaurants.each do |restaurant| %>
    <h3><%= link_to restaurant.name, restaurant_path(restaurant) %></h3>
    <h3><%= restaurant.address %></h3>
    <h3><%= restaurant.category %></h3>
    <h3><%= restaurant.phone_nbumber %></h3>
  </div>
<% end %>
